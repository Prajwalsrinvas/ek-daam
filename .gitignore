# secrets — never commit a real one
.env
.env.*
!.env.example
DESIGN.md

# runtime data
runs/

# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/
# uv.lock is COMMITTED: it is what makes `uv sync --frozen` in the Dockerfile
# build the versions this repo was actually tested against.

# node / vite
node_modules/
dist/
web/dist/
web/node_modules/
*.tsbuildinfo

# editors / os
.DS_Store
.idea/
.vscode/
